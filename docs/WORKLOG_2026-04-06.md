# Worklog 2026-04-06

## Mål for dagen
- Få boten over på en alltid-på server.
- Få X-nyheter inn raskere enn polling.
- Beholde dashboard + Telegram som kontrollflate.

## Hva som ble gjort
- Implementerte real-time `X filtered stream` worker i kodebasen.
- La til ny CLI-kommando: `run-x-stream`.
- Oppdaterte strategi til å behandle `x_stream:@...` som realtime-kilde.
- Oppdaterte `serverless` slik at direkte triggere kan kjøre uten full provider-krav i samme kall.
- La til tester for stream parsing og realtime entry-path.
- Oppdaterte `.env.example` og `README.md` for stream-oppsettet.

## Verifisering lokalt
- `uv run pytest -q` passerte med hele testpakken.
- Kompilering passerte med `python -m compileall`.

## Infrastruktur
- Opprettet Google Compute Engine VM (Ubuntu).
- Installerte avhengigheter og bootstrap på VM.
- Konfigurerte `systemd` service for boten.
- Byttet service-kommando fra `run-paper` til `run-x-stream`.

## Viktig feil som ble løst
- Service-script lastet ikke `.env` og støttet ikke `run-x-stream`.
- Fikset `deploy/server/run-service.sh` til å:
  - source `.env`
  - tillate `run-x-stream`
  - starte korrekt kommando via `uv`.

## Nåværende status
- `trading-bot.service` er oppe på VM.
- Service kjører `run-x-stream`.
- Logg viser tilkobling til X filtered stream.
- Tidligere `429 Too Many Requests` skjedde, men siste status viser running uten ny 429 i siste utdrag.

## Viktige driftsnotater
- Ikke kjør flere `run-x-stream` prosesser samtidig på ulike maskiner med samme token.
- Hvis `429` kommer tilbake:
  - stopp alle andre stream-prosesser
  - restart service
  - vurder fallback til `run-paper` + `X_RECENT_SEARCH`.

## Neste steg
- Legg inn automatisk fallback i kode:
  - stream først
  - fallback til polling ved vedvarende `429`
  - tilbake til stream når mulig.
- Flytt repo til riktig GitHub-konto/repo som eieren kontrollerer.
